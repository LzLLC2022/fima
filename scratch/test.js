const http=require('https'); const cheerio=require('cheerio'); http.get('https://stockcircle.com/portfolio/ray-dalio', {headers:{'User-Agent':'Mozilla/5.0'}}, res=>{let data=''; res.on('data', c=>data+=c); res.on('end', ()=>{ const $=cheerio.load(data); const h=[]; $('.share__top-box').each((i,el)=>{if(i>=10)return; const a=$(el).find('a.share__company-link'); if(!a.length)return; h.push(a.attr('href')); }); console.log(h); }); });
